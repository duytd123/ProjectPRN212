<Window x:Class="ProjectPRN212.NotificationDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NotificationDetailWindow" Height="400" Width="550"
        WindowStartupLocation="CenterScreen" Background="#F4F4F4">
    <Grid>
        <Border Width="500" Height="350" Background="White" CornerRadius="10"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Padding="20">
            <StackPanel Orientation="Vertical">
                <!-- Tiêu đề -->
                <TextBlock Text="Chi Tiết Thông Báo" FontSize="22" FontWeight="Bold"
                           Foreground="#333" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Nội dung thông báo -->
                <TextBlock x:Name="MessageTextBlock" FontSize="14" Foreground="#333" 
                           TextWrapping="Wrap" Margin="0,0,0,15"/>

                <!-- Ngày gửi -->
                <StackPanel Orientation="Horizontal" Margin="0,5,0,15">
                    <TextBlock Text="Ngày gửi: " FontWeight="SemiBold" FontSize="14" Foreground="#333"/>
                    <TextBlock x:Name="SentDateTextBlock" FontSize="14" Foreground="#333"/>
                </StackPanel>

                <!-- Thông tin xe (nếu có) -->
                <Border x:Name="VehicleInfoPanel" Visibility="Collapsed" Margin="0,10,0,15"
                        BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Thông tin xe" FontWeight="SemiBold" FontSize="16" Foreground="#333" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Biển số -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Biển số:" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,5,0,5"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="PlateNumberTextBlock" FontSize="14" Foreground="#333" Margin="0,5,0,5"/>

                            <!-- Thương hiệu -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Thương hiệu:" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,5,0,5"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="BrandTextBlock" FontSize="14" Foreground="#333" Margin="0,5,0,5"/>

                            <!-- Mẫu xe -->
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Mẫu xe:" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,5,0,5"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="ModelTextBlock" FontSize="14" Foreground="#333" Margin="0,5,0,5"/>

                            <!-- Năm sản xuất -->
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Năm sản xuất:" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,5,0,5"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="YearTextBlock" FontSize="14" Foreground="#333" Margin="0,5,0,5"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Nút đóng -->
                <Button x:Name="CloseButton" Content="Đóng" Width="120" Height="40"
                        Background="#007ACC" Foreground="White" FontSize="14" FontWeight="SemiBold"
                        HorizontalAlignment="Center" Margin="0,15,0,0" Cursor="Hand" Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>