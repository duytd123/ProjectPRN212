<Window x:Class="ProjectPRN212.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NotificationWindow" Height="400" Width="600"
        WindowStartupLocation="CenterScreen" Background="#F4F4F4">
    <Grid>
        <Border Width="550" Height="350" Background="White" CornerRadius="10"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Padding="20">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Width="530">
                <!-- Tiêu đề -->
                <TextBlock Text="Thông Báo" FontSize="24" FontWeight="Bold"
                           Foreground="#333" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Danh sách thông báo -->
                <ListBox x:Name="NotificationList" SelectionChanged="NotificationList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Margin="10">
                                <TextBlock Text="{Binding Message}" FontSize="14" TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="Black"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRead}" Value="True">
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                    <Setter Property="FontWeight" Value="Normal"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="{Binding SentDate, StringFormat='dd/MM/yyyy HH:mm'}" FontSize="12" Foreground="#666" Margin="0,5,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <!-- Nút đánh dấu tất cả là đã đọc -->
                <Button x:Name="MarkAllAsReadButton" Content="Đánh Dấu Tất Cả Là Đã Đọc" Width="200" Height="40"
                        Background="#007ACC" Foreground="White" FontSize="14" FontWeight="SemiBold"
                        HorizontalAlignment="Center" Margin="0,20,0,0" Cursor="Hand" Click="MarkAllAsReadButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>